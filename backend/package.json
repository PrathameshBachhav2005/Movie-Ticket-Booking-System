{
  "name": "movie_tickets_platform",
  "version": "1.0.0",
  "description": "Movie Ticket Booking System — Express + MongoDB backend",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "dev": "node --env-file=.env --watch --watch-path=src --watch-path=server.js --watch-preserve-output server.js",
    "start": "node --env-file=.env server.js"
  },
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^8.15.0",
    "stripe": "^22.2.2"
  }
}
